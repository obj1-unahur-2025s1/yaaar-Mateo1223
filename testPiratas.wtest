import piratas.*

describe "test piratas" {
  test "aaa" {
    const unPirata = new Pirata(items = ["test"])

    unPirata.monedas(5)
    unPirata.restarMonedas(10)
    assert.equals(unPirata.monedas(), 0)
  }
}
